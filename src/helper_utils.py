"""
helper_utils.py — Shared Utilities
=====================================

WHAT YOU'LL BUILD (same as NSFW helper_utils.py):
  • train_model() — reusable training loop with progress bars
  • evaluate_accuracy() — run model on dataloader, return accuracy
  • plot_confusion_matrix() — sklearn confusion matrix visualization
  • NestedProgressBar — epoch + batch progress tracking
  • set_seed() — reproducible experiments
  • Device auto-detection (CUDA / MPS / CPU)

COURSE REFERENCE:
  • NSFW helper_utils.py — copy the same patterns
"""
